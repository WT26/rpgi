#ifndef FUNCTIONS_HH
#define FUNCTIONS_HH

#include <vector>
#include <string>

using namespace std;

    bool start_menu();
    vector<string> split(const string& stringToSplit, char charToSplit);
    void endGame();

#endif // FUNCTIONS_HH
