#ifndef STARTMENU_HH
#define STARTMENU_HH

#include <vector>
#include <string>

using namespace std;

class StartMenu
{
public:
    StartMenu();
private:
    vector<string> commands_;
};

#endif // STARTMENU_HH
